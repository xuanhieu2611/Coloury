@AGENTS.md

# Coloury — project guide for Claude

AI-assisted, Lightroom-style photo editor. Full product spec is in `PRD.md` — read it for feature intent; this file is the working guide for *how the code is built and how to work in it*.

## What this is / current state

- **Built & verified: M1 + M2 (complete)** (PRD §8) — rendering engine (WebGL2 pipeline, live preview, export, all manual panels + histogram + undo/redo) **plus** M2: presets, before/after split-slider, crop/transform, **and** the §4.2 stretch controls (Clarity, Texture, Noise Reduction, Sharpen radius, Vignette midpoint/feather, Grain size). Straighten now auto-insets (no clamped-edge corners).
- **Built & verified: M3 (AI Auto Edit) — with M4's §4.3 UX** — real `claude-sonnet-5` vision call via a server route; style/intent input, "what changed" explanation, and re-roll ("Try another style" + cached looks). See the M3 notes below. Verified end-to-end with a live key: "warm and moody" returned a coherent grade (temperature +25, lowered exposure/highlights, deepened shadows, split-tone + vignette), the explanation matched the numbers, sliders moved, the WebGL center pixel shifted warm+dark, and undo reverted both pixels and sliders.
- **Built & verified: M4 (Polish) — complete.** The §4.3 pieces (style/intent, "what changed", re-roll) landed with M3; built-in presets with M2. M4 added the remaining polish: **large-image export guard** (GPU `MAX_TEXTURE_SIZE` clamp + proportional source downscale), **export resolution choice** (Original / long-edge presets / custom, PRD §4.5), **JPEG EXIF readout** (camera/lens/ISO/shutter/aperture/focal length in the toolbar, PRD §4.1), and **cross-browser HEIC decode** (heic2any fallback when native decode fails). See the M4 notes below.
- **Built & verified: Aesthetic filter pack (Phase 1) — teen/Pinterest looks.** Two new engine params — **`fade`** (lifted matte blacks) and **`halation`** (warm highlight glow) — wired recipe→shader→panel (Effects ▸ Film group)→AI. A curated **12-filter pack** (`src/lib/filters.ts`) across 4 categories (Digicam / Film / Fuji / Mood), surfaced as a **Tezza/VSCO-style filter strip** (`src/components/Filters.tsx`) with category tabs, **live per-filter thumbnail previews** (real WebGL render), and a **per-filter intensity slider** (`lerpRecipe` blends default↔filter). Verified with pixels: fade min 35→70 (whites held), halation → warm [246,237,234], Golden Hour neutral→[163,136,112], intensity 0%→neutral, undo restores. Deferred Phase 2 (overlays: date stamp, light leaks, dust/scratches/borders; 3D LUTs) is speced in **`FILM_ENGINE.md`**.
- **Built & verified: Film-engine Phase 2 — COMPLETE (steps 1–5).** The architectural jump above the single shader pass. `recipe.overlays` (date stamp, light leak, dust, border) is a compositing layer over the WebGL output; `recipe.lut` is a shader-side 3D LUT stage. See the Phase-2 notes below. All pixel-verified.
- **Built & verified: M5 — RAW support (complete).** Browser RAW decode via LibRaw-wasm → 8-bit sRGB canvas that flows through the existing pipeline unchanged (`src/lib/raw.ts`, `libraw-client.ts`, `public/libraw/`). See M5 notes below + **`M5.md`**. Verified with a real Sony ARW (6240×4168, EXIF mapped, edits move pixels, full-res JPEG export, build-safe).
- **Built & verified: M5 — Accounts (Supabase, identity only).** Email+password auth (`src/lib/supabase/*`, `src/proxy.ts`, `src/components/Account.tsx`); no-ops until env vars set. Live-verified against hosted project **`coloury`** (ref `cpmqbhzjsjielezftljl`) with env vars in `.env.local`: sign-up → confirm (email confirmation is ON; test user confirmed via SQL) → sign-in flips the header to the email + Sign out → reload keeps the session (`proxy.ts`) → sign-out returns to Sign in; `sb-…-auth-token` cookies set, no auth console errors. See `M5.md` §2. **Cloud storage + batch editing are deferred** (user chose accounts-only; see `M5.md` §3).
- **Built & verified: Authentic film-look pack (the Tezza/VSCO "WOW" play).** The 12-filter pack is now **LUT-driven** — each look's grade comes from a real film LUT (`src/lib/lut.ts`), not hand-typed slider guesses. New engine work: **real `.cube` loading** (`parseCube` + `sampleCube` trilinear resample; shader/renderer untouched) alongside a richer procedural film library (`bakeFilm`: lift/gamma/gain + split-tone + **skin-hue protection**). 8 LUTs (Portra 400, Kodak Gold, Classic Chrome, Superia, Teal·Orange, Faded Retro, B&W Film, Noir); rebuilt `FILTERS` across 4 categories (Film / Fuji / Cinema / Mono) with a `premium` flag (Pro badge, gating seam for the future subscription). **The hinge fix:** filters now *carry* their LUT (`Filters.applyFilter` stopped restoring `r.lut`) and `lerpRecipe` ramps `lut.amount` so the intensity slider dials film strength. New **one-tap before→after reveal wipe** (store `reveal` nonce → `EditorCanvas` auto-split animation, reduced-motion-aware). Verified on a real portrait (`.playwright-mcp/portrait.jpg`): Portra warms skin +14R/−4B while skin-protect keeps it alive, Superia boosts shirt greens, Teal·Orange cools shadows, B&W → R=G=B; intensity 0% = exact original, 50% interpolates, 100% full; undo restores; full-res export carries the grade; 13/13 `.cube` unit tests; build clean.
- **Built & verified: Simple/Advanced mode split (the Tezza front-door UX).** Filters are now the default hero instead of a dense pro cockpit. A UI-only `mode: 'simple' | 'advanced'` (store.ts, not part of the recipe, persists across image loads) gates the right rail: **Simple** shows only the creative zone (Auto Edit + a `hero`-sized Filters strip with a guide line), **Advanced** additionally reveals Histogram + Panels with a motivated `animate-reveal-panels` fade+rise. A segmented `ModeToggle` (sliding accent thumb) sits atop the rail. Verified in-browser: Simple is default and hides the cockpit; Advanced reveals it; toggle slides/persists; filter apply + intensity + grade persist across mode switches (grade lives in the recipe, mode is view-only).
- **Not built yet:** M5 cloud storage + batch editing (deferred); **subscription/paywall** (seam ready — `Filter.premium` + Pro badge; wiring Supabase-backed gating is the next isolated step). Film-engine future work: real `.cube` file *uploads* from users (parser exists), curated leak/dust texture packs (see `FILM_ENGINE.md`).
- **AI plan (done):** the Auto Edit uses a **real Anthropic vision call**, not a stub, exactly as the user wanted.

## Architecture (the one thing to understand first)

The **`EditRecipe`** (JSON, `src/lib/recipe.ts`) is the single source of truth. Nothing is baked into pixels until export. Everything flows one direction:

```
UI slider / (future) AI  →  update EditRecipe in Zustand store  →  WebGL renderer re-runs the shader  →  canvas
```

This means: **the AI integration (M3) does not touch the pipeline.** The vision model just returns an `EditRecipe`; validate it, clamp to `PARAM_RANGE`, drop it into the store. Keep `recipe.ts` as the exact contract the model must satisfy — if you change the schema, change the AI prompt and the shader together.

### File map
| File | Role |
|---|---|
| `src/lib/recipe.ts` | `EditRecipe` type, `defaultRecipe()`, `PARAM_RANGE` (now carries per-param `default`), HSL band definitions, `Crop` type + `defaultCrop`/`isIdentityCrop`, `clamp`/`cloneRecipe`, `normalizeRecipe` (merge partial→complete), **`recipeFromAi(partial)`** (M3 validation: untrusted model JSON → clamped full recipe; ignores curves/crop/garbage). **Single source of truth.** |
| `src/lib/aiEdit.ts` | M3 client side: downscales the preview to a ~1024px JPEG, POSTs to `/api/auto-edit`, and the `AutoEditRequest`/`AutoEditResult` contract. No API key in the browser. |
| `src/app/api/auto-edit/route.ts` | M3 backend (Next route handler): real `claude-sonnet-5` vision call. System prompt + JSON schema are **generated from `PARAM_RANGE`/`HSL_BANDS`** so they can't drift from the contract; parses the model JSON, runs it through `recipeFromAi`, returns `{recipe, explanation}`. Reads `process.env.ANTHROPIC_API_KEY`. |
| `src/lib/crop.ts` | Crop geometry: `computeCropTransform()` turns `recipe.crop` into an affine sampling map (`origin`,`u`,`v`) + output dims; `ASPECT_PRESETS`. |
| `src/lib/exif.ts` | M4: dependency-free JPEG EXIF reader (`parseExif(ArrayBuffer)` → `ExifData` or `null`) + `summarizeExif()` one-liner. Parses only the APP1/TIFF tags we surface (make/model/lens/ISO/shutter/f-number/focal length/date); forgiving — any failure returns `null` so the UI just omits the row. Only JPEG carries EXIF in our accepted formats. |
| `src/lib/presets.ts` | `Preset` type, 4 built-in starters, localStorage load/save/delete for user presets. |
| `src/lib/gl/shaders.ts` | GLSL ES 3.00 vertex + fragment. Pipeline is **10 stages in PRD §5.2 order**, each its own function; `main()` composes them. |
| `src/lib/gl/renderer.ts` | `Renderer` class (WebGL2, uniforms, curve LUT texture) + `exportImage()` (full-res offscreen render). |
| `src/lib/curves.ts` | Control points → 256-entry LUT (monotone cubic), composed into an RGBA texture. |
| `src/lib/store.ts` | Zustand store: image, recipe, undo/redo history, image decode + preview downscale. |
| `src/lib/histogram.ts` | Separate store + 64-bin compute for the live histogram. |
| `src/components/` | `Uploader`, `EditorCanvas`, `Panels`, `Slider`, `CurveEditor`, `Histogram`, `Toolbar`, `Presets`, `CropOverlay` (crop rect + handles), `CropBar` (aspect/straighten/rotate toolbar), `AutoEdit` (M3 panel at top of side-col: intent input, Auto Edit / re-roll, explanation, cached looks). |
| `src/app/{page,layout}.tsx`, `globals.css` | Shell + Tailwind v4 theme (`@theme` tokens in `globals.css`). Shared button/chip class strings in `src/lib/ui.ts`. Residual CSS only for range thumbs + crop-handle `::after`. |

### Pipeline order (do not reorder without reason — mirrors Lightroom)
white balance → exposure → tone (contrast/highlights/shadows/whites/blacks) → custom curve (LUT) → HSL → vibrance/saturation → split toning → sharpening → vignette → grain.

## Commands
- `npm run dev` — dev server (Next 16 + Turbopack) on :3000.
- `npm run build` — production build (also runs TS). Use to catch build-time issues.
- `npx tsc --noEmit` — typecheck.

## How to verify changes (expected workflow)

**Runtime observation, not tests.** This app's whole surface is pixels + WebGL, so:
1. Start the dev server, drive it in a browser (Playwright MCP).
2. Confirm edits by **reading canvas pixels** via `getImageData`, not by eyeballing — e.g. exposure +3 must ~8× the pixel; saturation −100 must collapse to grayscale (R=G=B).
3. Screenshot for the visual, check the console for errors, test export produces a non-empty Blob.
- Playwright gotcha: file uploads must live **inside the repo root** (e.g. `.playwright-mcp/`), not `/tmp` — uploads from outside allowed roots are denied.

## Lessons / rules — log everything here (per user request)

The user explicitly asked: **log every mistake, rule, or significant preference here** so it isn't repeated. Append to this list whenever something is learned.

- **[bug, fixed] Undo/redo must snapshot the PRE-edit recipe.** First attempt had `commit()` push the *current* (post-edit) recipe, so undo after a slider drag was a no-op. Fix: the store captures a `pending` snapshot on the first uncommitted (`commit=false`) edit of a drag, and `commit()` pushes *that*. Slider/curve pattern: fire `update(mut, false)` during drag, `commit()` on release. If you touch history, re-verify undo→redo actually moves pixels.
- **[rule] Verify by observation, every non-trivial change.** Don't report done from a passing typecheck/build alone — drive the running app and read pixels. This is how the undo bug was caught.
- **[gotcha] Next.js 16 is not your training data.** Breaking changes vs. older Next. Read `node_modules/next/dist/docs/` before writing Next-specific code (see AGENTS.md).
- **[gotcha] Project dir is `Coloury` (capital C).** npm rejects capitals in package names, so `create-next-app .` fails; the package name in `package.json` is lowercase `coloury`. Scaffold into a temp lowercase dir and move if re-scaffolding.
- **[preference] Tailwind v4 + existing dark tokens.** Lightroom-style dark theme lives in `@theme` (`globals.css`); use utilities in components. Shared patterns (`btn`, `btnPrimary`, …) live in `src/lib/ui.ts`. Keep residual CSS only for things utilities can't express (range thumbs, crop-handle `::after`).
- **[preference] Recipe stays the contract.** Any new adjustment = add to `EditRecipe` + `PARAM_RANGE` + a shader stage + a Panel control, in that mental order. Don't add UI-only edits that bypass the recipe.
- **[M2, verified] Crop/transform lives in `recipe.crop` as geometry, applied as an affine sampling transform — never a second pass.** `computeCropTransform` (crop.ts) maps output→source as `sUv = uSampO + vUv.x*uSampU + vUv.y*uSampV`; the renderer resizes the drawing buffer to the crop's output dims each `render()` and sets those 3 uniforms. Straighten rotates **about the image center** so a CSS `rotate()` on the canvas (transform-origin = center) is a truthful live preview. Orientation (90° turns) = rotate the output-op square + swap out dims. Verified: 1:1→600×600, one 90° turn→dims swap, export carries crop to full res (600×800 file).
- **[M2, gotcha] Once crop sampling exists, every `uImage` read must use the mapped source coord, not `vUv`.** The fragment sets a global `gSrc` in `main()`; `sharpen()` samples `gSrc ± uTexel` (source space). `vignette`/`grain` intentionally stay in `vUv` (output/framing space). Miss this and sharpen samples the wrong pixels under a crop.
- **[M2, decision] Before/after keeps the crop framing.** `render(recipe, bypass, split)` computes geometry from the *real* `recipe.crop` for both passes and only zeroes the color params for the original — so the split slider / hold-to-compare don't make the frame jump. Split is done with a GL **scissor** re-draw of the original into the left region (single canvas, one frame), divider measured against the canvas element box (aspect-preserved, no letterbox).
- **[M2, gotcha] Crop edits use the Slider drag/commit history pattern.** `applyCrop(mut, false)` during a handle drag (sets `pending`), `commit()` on release; discrete ops (rotate/aspect/reset) do `update(mut,false)` then `commit()` for one undo step. Verified undo/redo moves the crop dims.
- **[M2] Presets = named `EditRecipe`s.** Applying one is just `setRecipe(cloneRecipe(preset.recipe))` (commits history). Built-ins in code, user presets in `localStorage` (`coloury.presets.v1`), hydrated after mount (client-only). B&W preset verified via pixels (R=G=B). `loadUserPresets` runs each through `normalizeRecipe` so presets saved before a field existed stay complete — **use `normalizeRecipe` for the M3 AI's partial recipe too.**
- **[M2, verified] §4.2 stretch controls added.** Recipe fields: `clarity`,`texture` (Presence), `sharpenRadius`,`noiseReduction` (Detail), `vignetteMidpoint`,`vignetteFeather`,`grainSize` (Effects). Some have **non-zero defaults** (sharpenRadius 1, vignette mid/feather 50, grainSize 25) — `PARAM_RANGE[k].default` drives the Slider's double-click reset and the section Reset; `ScalarSlider` reads it. Shader: clarity/texture/denoise/sharpen all share one `srcBlur(radPx)` source-neighborhood helper (samples `gSrc`, not `vUv`); clarity is midtone-masked large-radius luma highpass, denoise pulls toward the blur, sharpen radius scales the kernel.
- **[M2, verified] Straighten auto-insets so corners never sample outside the image** (`fitScale` in crop.ts): shrink the rect about its center until all 4 rotated corners fit in [0,1], scaling output dims by the same factor. Verified: 400²+15° → 327² output, corner regions have real detail (variance ~4100, zero black) — no letterbox, no clamped-edge smear. Identity/1:1/edge crops give scale 1 (no unwanted shrink). Note: the live CSS-rotate preview shows the *un-inset* rect, so the framing tightens slightly on Done — acceptable.
- **[rule, verify] Spatial ops need a high-frequency test image.** Clarity/texture/sharpen/noise produce ~0 highpass on a smooth gradient, so they can't be verified on `test.jpg`. Generate a checkerboard+noise PNG (see `.playwright-mcp/checker.png`, made with Python PIL) and assert on **central-region luminance variance**: texture/clarity/sharpen raise it, noise reduction lowers it (and restores to the exact baseline when zeroed). Vignette params verified via a mid-radius band's mean.
- **[M3, decision] The AI never touches the pipeline — it only returns a recipe.** Flow: `AutoEdit` → `requestAutoEdit` (downscale to ~1024px JPEG) → `POST /api/auto-edit` → real `claude-sonnet-5` vision call → parse JSON → **`recipeFromAi` clamps/validates** → `setRecipe(recipe)` (commits to undo history, so the AI edit is fully editable + undoable like any manual edit). Model config: `claude-sonnet-5`, adaptive thinking, effort `medium`, non-streaming, `max_tokens` 8000. Auth: `ANTHROPIC_API_KEY` in `.env.local` (gitignored), read server-side only.
- **[M3, rule] `recipeFromAi` is the trust boundary — the model output is untrusted.** It starts from `defaultRecipe()`, copies only recognized finite numbers (clamped to `PARAM_RANGE`/band ranges), deep-merges partial HSL bands (so a band with only `saturation` keeps default hue/luminance — **not** NaN, which `normalizeRecipe`'s shallow band merge would have produced), and **ignores curves + crop entirely** (a bad curve/crop is far more destructive than a clamped slider). Malformed/`null`/string input → full default (no-op, never a crash). Verified with 18 node assertions (out-of-range clamp, garbage ignored, partial band, split-tone ranges, curves/crop dropped).
- **[M3, rule] Prompt schema is generated from `PARAM_RANGE`/`HSL_BANDS`, not hand-written.** `scalarRangeLines()` in the route builds the schema/ranges block from the contract so the prompt can't silently drift when a param is added. If you add an AI-controllable field: add it to `PROMPTED_KEYS` (route) **and** `AI_SCALAR_KEYS` (recipe.ts) — the prompt and the validator must agree.
- **[M3, verified] Proven end-to-end with a live key.** build + typecheck clean; `recipeFromAi` unit tests (18/18); running app with a real `ANTHROPIC_API_KEY` — `AutoEdit` downscales+base64-encodes the image (~28KB), POSTs `{image, mediaType, style}`, the `claude-sonnet-5` call returns a coherent recipe + matching explanation, sliders move, the WebGL center pixel shifts (edited `[104,95,41]` warm+dark vs original `[127,127,57]`), and ⌘Z reverts both pixels and sliders (recipe is on the undo stack via `setRecipe`). The empty-key path surfaces the exact "not configured" error. Note the model correctly identified `test.jpg` as a synthetic gradient — proof the image reached the vision model.
- **[M4, verified] Large-image export = downscale the SOURCE, not just the output.** The GPU's `MAX_TEXTURE_SIZE` (16384 on this Mac) caps both the uploaded source texture and the output canvas — a >16k-px image would silently fail `texImage2D`. `planExport(recipe, w, h, desiredLongEdge)` (renderer.ts) resolves one `sourceScale = min(requestScale, capScale)` (never upscales), and `exportImage` draws the source onto a 2D canvas at that scale **before** `setImage`. Because output dims derive from source dims via the crop transform, scaling the source hits the target output size exactly and keeps both under the cap. `gpuClamped` flags when the GPU (not the user's choice) is the binding constraint → the export dialog shows a red "capped to this GPU's max texture size" note. Verified: real 1024×683 JPEG out of a 1200×800 source (blob decoded to confirm pixel dims); clamp math unit-checked (20000px → 16384, user-chosen size binds when smaller). Preview stays capped at 2048px (`MAX_PREVIEW`, store.ts) — unchanged.
- **[M4, verified] Cross-browser HEIC via lazy heic2any.** `loadImageFile` (store.ts) tries native `<img>` decode first (Safari's is fast); on failure for a HEIC/HEIF file it dynamic-`import()`s `heic2any` (wasm, client-only — keeps it out of the SSR bundle and off the JPEG/PNG path) to convert to a JPEG blob, then decodes that. Verified in Chromium (no native HEIC): `sample.heic` → 1200×800 canvas with correct gradient pixels, no console errors. EXIF is JPEG-only, so HEIC just shows no metadata row (expected).
- **[M4, gotcha] EXIF only for JPEG; parse from the raw bytes, not the decoded image.** `parseExif(await file.arrayBuffer())` runs on the original File (guarded to `image/jpeg`/`.jpg`), since the `HTMLImageElement`/canvas has no metadata. Keep it dependency-free and failure-tolerant (returns `null`). Verified the full readout from a piexif-authored test JPEG: `SONY ILCE-7M3 · FE 55mm F1.8 ZA · 55mm · f/1.8 · 1/250s · ISO 400`. Small-file sizes now render as KB (`formatBytes` in Toolbar) so a 43KB file isn't "0.0 MB".
- **[filters, verified] Filters are named recipes; the strip renders REAL thumbnails via a throwaway Renderer.** `src/components/Filters.tsx` downscales `image.preview` to a ~160px base, spins up one offscreen `Renderer`, and `render()`s each filter to grab `toDataURL` — so previews are the true grade, not CSS fakes. Applying uses `lerpRecipe(defaultRecipe(), filter.recipe, intensity/100)` and **preserves the user's `recipe.crop`** (copies all fields but restores crop) so picking a filter never resets a crop. Intensity uses the Slider drag/commit pattern (`update(...,false)` during drag, `commit()` on release) = one undo step. Thumbnails regenerate on image change only.
- **[filters, gotcha] `fade`/`halation` are new AI-controllable params — added to BOTH `AI_SCALAR_KEYS` (recipe.ts) and route `PROMPTED_KEYS` + `KEY_NOTE` (per the M3 sync rule). Also added `grain`.** Shader order: `sharpen → halation → fade → vignette → grain`. Halation samples `srcBlur(9.0)` off `gSrc` (source space, like clarity/texture — glow keys off the *original* highlights, an accepted approximation), screen-blends warm `(1.0,0.55,0.42)`. Fade is `c*(1-lift)+lift`, `lift=uFade*0.16` (max black floor ~41/255, white held).
- **[phase2, decision] Overlays are a COMPOSITING LAYER over the WebGL output, never shader color math.** `recipe.overlays` (new top-level section in `recipe.ts`) is the contract; `src/lib/overlays.ts` owns the single `drawOverlays(ctx, W, H, overlays)` used by BOTH the live-preview overlay canvas (`EditorCanvas`) and export-time 2D compositing (`exportImage` draws the GL canvas onto a fresh 2D canvas at output dims, then `drawOverlays`) — so preview == export, at any size. Chose option (a) from `FILM_ENGINE.md` (2D compositing), not a second GL pass. `normalizeRecipe` deep-merges `overlays.dateStamp` (backward compat for pre-Phase-2 presets); `recipeFromAi` ignores overlays (manual framing, not AI-graded); `Filters.applyFilter` preserves `overlays` like it preserves `crop`.
- **[phase2, decision] Borders EXPAND the output canvas around the image; they don't inset/crop.** `computeFrame(imgW,imgH,border)` (overlays.ts) returns the expanded `{outW,outH}` + the `region` the image occupies; `composeOverlays(ctx,src,overlays)` draws frame bg + film sprockets + the image (into region) + leak/dust/stamp. **Preview** (`EditorCanvas`): when a border is active the overlay canvas composites the whole framed result and the GL canvas is hidden (`opacity 0`, still rendered for the histogram readback); no border → overlay is GL-sized and just draws leak/dust/stamp transparently over the visible GL image. **Export** composites onto a 2D canvas at `computeFrame` dims — 2D canvases have no `MAX_TEXTURE_SIZE`, so frame padding can't hit the GPU cap (the GL image is still capped by `planExport`). Verified Polaroid/film preview == export dims to the pixel.
- **[phase2, decision] Light leaks + dust are PROCEDURAL and DETERMINISTIC — no binary assets.** Leak = edge/corner radial gradient, `screen`-blended, clipped to the image region. Dust = dark specks (`multiply`) + near-vertical bright scratches (`screen`), seeded with a **fixed** `mulberry32` seed so the live preview and export draw the identical wear (a random seed would make export not match preview). Both live under `recipe.overlays`.
- **[phase2, verified] 3D LUT = tiled 2D texture on TEXTURE2, trilinear in-shader; LUTs are self-authored/procedural (no proprietary .cube).** `src/lib/lut.ts` bakes an N=17 cube (N slices laid across → texture `N*N × N`) via JS color transforms into RGBA bytes; `renderer` uploads it to `TEXTURE2` and **rebuilds only when `recipe.lut.id` changes** (not every frame). Shader `applyLut` (after split-tone, before effects) samples two blue-slices with the `(slice*N + r*(N-1) + 0.5)/(N*N)` / `(g*(N-1)+0.5)/N` coordinate math (LINEAR filtering gives bilinear r,g within a slice; blue axis lerped manually) and `mix`es by `uLutAmount`. Gated by `uUseLut` (`id!=='none' && amount>0`) so bypass/ZERO_RECIPE passes skip it. Verified B&W→R=G=B, teal/kodak shifts, none/0 = no-op. To add real film LUTs later: swap the data source in `buildLutTexture`, shader unchanged.
- **[phase2, rule] New overlay/LUT fields must be synced across the contract.** Adding an overlay or LUT field means: `EditRecipe` + `default*()` + `normalizeRecipe` deep-merge (backward compat for pre-Phase-2 presets/localStorage) + a Panels control, and preserve it in `Filters.applyFilter`. `recipeFromAi` still ignores overlays+lut (manual/graded tools). LUTs render on `TEXTURE2` (image=0, curve=1, lut=2). **Note (superseded by the film-pack work below):** `Filters.applyFilter` now preserves only `crop` + `overlays` (framing), NOT `lut` — the LUT *is* the grade a filter carries.
- **[phase2, verified] Date stamp = 7-segment glyphs drawn from rects, no font asset.** `overlays.ts` renders digits/space/apostrophe from segment rectangles + a soft `shadowBlur` LED bloom, sized as a fraction of the frame short-edge so it scales to any export. `text` holds the LITERAL characters (auto-filled from EXIF `DateTimeOriginal` or today when first enabled) so `exportImage` needs no metadata — the recipe fully describes the draw. **Verify overlays with pixels, but pick a threshold the image can't fake:** exif.jpg is a warm gradient, so a loose "amber" test counted the gradient; a tight LED threshold (`r>225 && g in 85..160 && b<80`) isolated the stamp (1239 px in the correct corner, 0 elsewhere). Preview overlay canvas dims are synced to the GL canvas drawing buffer each frame and it's `pointer-events-none absolute inset-0 object-contain`; hidden during crop mode and hold-to-compare (it's an edit).
- **[gotcha, glsl] Backticks inside the GLSL comment break the JS template literal.** `shaders.ts` wraps GLSL in a `` `...` `` template string, so a stray backtick in a `//` comment (e.g. around a word for emphasis) silently terminates the string → cascading TS1005 parse errors far from the real spot. Never use backticks in shader comments.
- **[preference, UI] Coloury visual language = colorist's desk, not generic SaaS.** Keep the Lightroom dark chrome (deep charcoal panels, dense controls, photo-first canvas). Signature is the HSL **spectrum bar** under the Coloury wordmark + empty-state wash (cool left / warm right). Type: **Syne** (brand) + **IBM Plex Sans/Mono** (UI/values) — not Inter, not purple-violet accent packs. Accent stays cool daylight blue (`#6bb3ff`). Prefer SVG icons over emoji; visible `:focus-visible` rings; `prefers-reduced-motion`; stack canvas-over-panels below `md`.
- **[M5/RAW, verified] RAW decode = LibRaw-wasm → canvas → existing pipeline; nothing else changes.** `decodeRaw` (raw.ts) demosaics to 8-bit sRGB and paints a full-res canvas; `LoadedImage.element` is now `HTMLImageElement | HTMLCanvasElement` (both are `TexImageSource`, so the renderer/export are untouched). `store.loadImageFile` branches on `isRaw(file)`. LibRaw metadata → `ExifData` via `toExif` (so the toolbar readout works for RAW too). Verified with a real Sony ARW: 6240×4168, EXIF `Sony ILME-FX30 · E 18-135mm · f/5 · 1/125s · ISO 800`, exposure+3 clips center pixel to white, full-res 8.1MB JPEG export. Sample RAW: `.playwright-mcp/example-sony.ARW`.
- **[M5/RAW, gotcha — cost real time] `libraw-wasm`'s npm entry DEADLOCKS `next build` (Turbopack).** Its `new Worker(new URL('./worker.js', import.meta.url))` makes Turbopack try to bundle the worker + 1.4MB wasm at build time and hangs forever at "Creating an optimized production build" at 0% CPU (dev is fine). Bisected: stubbing the import → build in ~950ms. **Fix:** vendor `worker.js`/`libraw.js`/`libraw.wasm` into `public/libraw/` (via `scripts/copy-libraw.mjs`, run on postinstall/predev/prebuild) and spawn the worker from a **plain string path** `new Worker('/libraw/worker.js')` in the vendored wrapper `src/lib/libraw-client.ts` (Turbopack only traces the `new URL(...,import.meta.url)` form). Types still come from `libraw-wasm` via a type-only import. **Do NOT** revert `raw.ts` to `import('libraw-wasm')`.
- **[M5/accounts, verified] Supabase auth = identity only (no cloud storage yet).** `src/lib/supabase/{env,client,server,middleware}.ts` + `src/proxy.ts` + `src/components/Account.tsx`. All no-op until `NEXT_PUBLIC_SUPABASE_URL` + publishable/anon key are set, so the app runs pre-setup. Packages pinned: `@supabase/ssr@0.12.1`, `@supabase/supabase-js@2.110.3`. **Live-verified** against hosted project `coloury` (ref `cpmqbhzjsjielezftljl`, created via the Supabase MCP): full sign-up → SQL-confirm → sign-in → reload-persists → sign-out cycle with `sb-…-auth-token` cookies (see `M5.md` §2). Security: only the publishable/anon key in `NEXT_PUBLIC_*`; never the service-role key. Email confirmation is ON by default; the MCP can't toggle auth config, so either confirm users via SQL or turn "Confirm email" off in the dashboard.
- **[Next 16, gotcha] `middleware.ts` is deprecated → `proxy.ts`.** Next 16 renamed the file convention; export a `proxy` function (not `middleware`). We use `src/proxy.ts`. It warns but still builds under the old name — don't recreate `middleware.ts`.
- **[gotcha] Don't run `next dev` and `next build` at once — they fight over `.next` and the build hangs.** Kill the dev server before a production build (or use a separate dir).
- **[film-pack, decision] The filter pack is LUT-DRIVEN — the LUT is the grade, not slider guesses.** Each `FILTERS` entry (filters.ts) sets `recipe.lut = {id, amount}` (from `src/lib/lut.ts`) plus a light dressing of grain/halation/fade/vignette. This is the "authentic look" strategy: parametric sliders can't reach film hue-crosstalk / skin roll-off; a real LUT can. `lut.ts` now has TWO sources feeding the same tiled `TEXTURE2` texture: **procedural film specs** (`bakeFilm` — declarative `wb`/`contrast`/`lift`/`gamma`/`gain`/`sat`/`hueSat`/`skinProtect`/split-tone/`fade`, tuned by eye) and **real `.cube` files** (`parseCube` → `sampleCube` trilinear resample → `cubeToLutDef`). `buildLutTexture` is unchanged — every LutDef exposes a `transform` closure, so `.cube` LUTs just implement it as a grid lookup. **Shader + renderer stay untouched** (the renderer still keys the texture rebuild on `recipe.lut.id`). Verify `.cube`/bake with node type-stripping (`node x.test.mts`, lut.ts is import-free) — 13 assertions cover parse/trilinear/identity/film shifts/greyscale.
- **[film-pack, HINGE — the enabling fix] A filter must CARRY its LUT; the intensity slider dials it.** Two coupled changes that made LUT-driven looks possible: (1) `Filters.applyFilter` stopped doing `r.lut = lut` (it used to *restore* the user's LUT) — it now adopts `target.lut` and preserves only `crop` + `overlays`. (2) `lerpRecipe` (filters.ts) now interpolates the LUT: when `a.lut.id !== b.lut.id` (the usual apply-from-default case) it ramps `b`'s LUT in from `amount 0` (`out.lut.amount = t>0 ? b.lut.amount*t : a.lut.amount`, `out.lut.id = t>0 ? b.lut.id : 'none'`), so intensity 0% = exact original (LUT off), 100% = full film. Pixel-verified on a real portrait: at 0% skin returns to the exact source pixel, 50% interpolates monotonically. If you add a new recipe object-field that a filter should carry vs. preserve, decide it HERE (grade → carry; framing → preserve).
- **[film-pack, verified] Verify color LUTs on a REAL photo with a KNOWN skin swatch — not test.jpg's gradient.** Generated `.playwright-mcp/portrait.jpg` (PIL: warm skin oval ~[214,170,140], brown hair, saturated-green shirt, cool bg, + pixel noise) so skin/greens/shadows are each assertable. Read GL-canvas pixels by `drawImage`-ing onto a 2D canvas (WebGL context can't `getImageData` directly). Confirmed: Portra warms skin (+14R/−4B) while `skinProtect` keeps it saturated (not greyed), Superia pushes shirt greens (G 151→164), Teal·Orange cools bg/shadows, B&W/Noir → R=G=B. **Wait ~850ms after tapping a filter before reading** — the reveal wipe (below) animates the split for ~650ms, so an immediate read catches a half-wiped frame.
- **[film-pack, UI] One-tap before→after reveal wipe = the "WOW" surface.** Store carries a `reveal` nonce (`triggerReveal()`); `Filters.select` bumps it after applying; `EditorCanvas` watches it and animates a one-shot auto-split from all-original → all-edited (~650ms ease-out cubic), honoring `prefers-reduced-motion` (skips the animation). It reuses the existing split-render path (`render(recipe, false, splitFrac)`) and takes precedence over the manual Split slider. Purely presentational — no recipe/history effect.
- **[simple/advanced, decision] `mode` is UI-only view state, NOT part of the recipe.** It lives in the editor store (`store.ts`) beside `image`/`recipe` but is deliberately omitted from `setImage`'s `set({...})` so a user's Simple/Advanced choice persists when they open the next photo. Nothing about the grade depends on mode — switching modes never touches the recipe, so a filter/intensity applied in one mode is intact in the other. Simple shows `<AutoEdit/>` + `<Filters hero/>`; Advanced additionally renders `<Histogram/>` + `<Panels/>` inside an `.animate-reveal-panels` wrapper (fade+rise, collapses under the global reduced-motion reset). `ModeToggle` thumb: `w-[calc(50%-4px)]` inside a `p-1` pill, advanced = `translateX(calc(100% + 4px))` (the +4px offsets the padding so the thumb lands exactly on slot 2). `Filters` takes a `hero` prop that only bumps the thumbnail edge (68→84px) + adds a guide line; the apply/lerp path is unchanged.
- **[gotcha, dev] Turbopack HMR can reset the Zustand store mid-session.** After editing store-touching files, a fast-refresh may snap `mode` back to its initial `'simple'` while leaving the image loaded — looked like a toggle bug but wasn't. When verifying UI state after code edits, re-drive from a clean toggle click rather than trusting state carried across an HMR boundary.
- **[film-pack, monetization seam] `Filter.premium` + a "Pro" badge on the thumbnail is the subscription seam — NOT yet a paywall.** Premium looks still apply today (badge only); wiring Supabase-backed gating (block + upsell) is the next isolated step. Keep the free set genuinely good (per `FILM_ENGINE.md`). Target model is a subscription (user's call).
